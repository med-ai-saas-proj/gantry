apiVersion: 1

datasources:
  - name: jaeger
    type: jaeger
    url: http://jaeger:16686
    access: proxy
    jsonData:
      pdcInjected: false

  - name: loki
    type: loki
    url: http://loki:3100
    isDefault: true
    access: proxy
    jsonData:
      pdcInjected: false

  - name: prometheus
    type: prometheus
    url: http://prometheus:9090
    access: proxy
    jsonData:
      pdcInjected: false
